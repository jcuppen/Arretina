"use strict";

const ELEMENT_TYPES = {
  scalar: 'scalar',
  array: 'array'
};

const INPUT_TYPES = {
  length: 'length',
  color: 'color',
};

const OPTION_TYPES = {
  parity: 'parity',
  comparison: 'comparison',
  hideValue: 'hide-value',
};
